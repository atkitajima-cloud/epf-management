# リポジトリの作業規則

## 正本

- マークダウンと版管理履歴を唯一の正本とする。
- データベースや画面専用の永続データを追加しない。
- `tasks/*.md` は一つの作業につき一ファイルとし、先頭の属性欄に必要な項目を保つ。
- 画面の都合で、マークダウンのデータ構造や意味を壊さない。

## 情報のつながり

- 背景情報、要求、作業、計画のつながりを保つ。
- 作業には対応する要求を明記する。
- WBSは `tasks/*.md` から作る派生表示であり、正本として直接編集しない。
- 事実と推測、提案を明確に分ける。

## 人工知能の安全性

- 人工知能は既存の要求や決定事項を勝手に変更しない。
- 明示的な作業追加以外は、原則として変更案を示し、承認を待つ。
- Codexには読み取り専用で分析させ、書き込みはアプリ側で検証して行う。

## 変更の管理

- 大きな変更の前に `plans/` に計画を作成または更新する。
- 変更履歴と利用者の未記録変更を尊重する。
- アプリが利用者の操作を自動で記録・送信しない。
- 変更後はマークダウンの読み書き、通信口、主要な画面操作を確認する。

## 計画に基づく開発

- 小さな文言修正を除く機能追加、データ構造変更、外部連携、運用変更の前には、必ず `plans/` に計画を作成または更新する。
- 計画には目的、対象範囲、対象外、設計判断、実装手順、確認方法、危険性、未決事項を記載する。
- 利用者が「計画のみ」「計画作成まで」と指定した場合は、計画以外のコード、設定、マークダウンのデータ、版管理操作を変更せず、確認を待つ。
- 計画の確認後も、実装開始は利用者の明示的な承認を受けてから行う。承認範囲を超える実装は行わない。
- 計画で提案した機能は、実装済みであるかのように画面、説明書、回答で扱わない。

## 作業ターンごとの記録

- 変更を伴う作業ターンは、完了前にそのターンで意図した変更を必ず版管理へ記録する。変更がない回答だけは記録しない。
- 記録前に対象ファイル、差分、確認結果を確かめ、無関係な変更を混在させない。
- 記録の題名は日本語を必須とし、そのターンの変更内容が履歴から分かる簡潔な命令形にする。
- この規則はCodexの作業に適用するものであり、Webアプリが利用者の操作を自動で記録・送信することを意味しない。